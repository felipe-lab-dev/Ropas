# Producción (Next.js prefiere .env.production sobre .env en builds prod).
# Dejamos NEXT_PUBLIC_API_URL vacío para que lib/tenant.ts resuelva en runtime
# desde window.location (api.<tenant>.tienda.enkihubs.com).
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_TENANT_CODE=
NEXT_PUBLIC_APP_NAME=Ropas
